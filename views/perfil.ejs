<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Perfil</title>
  <link rel="stylesheet" href="css/perfil.css">
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css" rel="stylesheet">
</head>
<body>
  <div class="contenedor">
    <!-- Barra superior -->
    <div class="barra-superior">
      <div class="menu-icono" id="menu-icono"></div>
      <div class="titulo">To-Do List</div>
    </div>

    <!-- Barra lateral -->
    <div class="barra-lateral" id="barra-lateral">
        <div class="cerrar" id="cerrar-lateral">&times;</div>
        <ul>
            <li><a href="/tareas"><i class="fas fa-home" style="font-size: 30px;"></i><p>Inicio</p></a></li>
            <li><a href="/calendario"><i class="fa-regular fa-calendar" style="font-size: 30px;"></i><p>Calendario</p></a></li>
            <li><a href="/crear"><i class="fa-solid fa-plus" style="font-size: 30px;"></i><p>Crear</p></a></li>
        </ul>
    </div>
    
    <div class="perfil-container">
        <h2>Perfil</h2>
        <form id="perfil-form" action="/perfil" method="POST">

            <div class="form-item">
                <label for="nombreUsuario">Nombre de Usuario:</label>
                <input type="text" id="nombreUsuario" name="name" value="<%= user.nombre %>" required>
            </div>
            <div class="form-item">
                <label for="biografia">Descripción:</label>
                <textarea id="biografia" name="descripcion" placeholder="Escribe algo sobre ti"><%= user.descripcion %></textarea>
            </div>


            <div class="form-item">
                <label for="correo">Usuario:</label>
                <input type="text" id="correo" name="user" value="<%= user.usuario %>" required>
            </div>
            <div class="form-item">
                <label for="telefono">Teléfono:</label>
                <input type="tel" id="telefono" name="tel" value="<%= user.telefono %>" required>
            </div>
            <div class="form-item">
                <label for="password">Contraseña:</label>
                <input type="password" id="password" name="password" value="<%= user.contraseña %>" required>
            </div>

            <button type="submit" class="btn-submit">Guardar Perfil</button>

        </form>
    </div>
  </div>
  <script>
            
    document.getElementById('perfil-form').addEventListener('submit', function(event) {
        event.preventDefault();
        alert('Perfil guardado exitosamente.');
    });
</script>
  <script src="./frontmain.js"></script>
</body>
</html>
